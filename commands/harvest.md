---
description: Harvest this repo's git activity + session context into ranked article-topic ideas, then write chosen ones as content seeds.
---

Invoke the `harvest` skill (via the Skill tool) and follow it exactly — it has the full harvest → ideate → (human pick) → seed procedure. Pass along any arguments given here: $ARGUMENTS

(The skill is the single source of truth for this procedure — kept here as a thin pointer so `/harvest` and natural-language triggers like "what should I write about" invoke the identical flow instead of two copies drifting apart.)
